ARG BASE_IMAGE=toluva-worker:queue-v4-39b1b9a
FROM ${BASE_IMAGE}

# This maintenance image is valid only when Dockerfile, pyproject.toml, and
# uv.lock are unchanged from BASE_IMAGE. It replaces application source while
# preserving the previously verified pinned runtime and model layers.
COPY --chown=10001:10001 services/pipeline/src /app/services/pipeline/src
